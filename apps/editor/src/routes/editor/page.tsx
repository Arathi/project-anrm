import { Flex } from 'antd';
import FileTabs from '@/components/file-tabs';

const EditorPage = () => {
  return (
    <Flex vertical>
      <FileTabs />
    </Flex>
  );
};

export default EditorPage;
